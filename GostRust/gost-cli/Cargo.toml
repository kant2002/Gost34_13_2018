[package]
name = "GostRust"
version = "0.1.0"
edition = "2021"

[dependencies]
gost = { path = "../gost" }
anyhow = "1.0"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "my_benchmark"
harness = false